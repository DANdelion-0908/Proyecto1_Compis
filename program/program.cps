let x: integer = 5;
let y: boolean = (x < 10 && x > 0);

if (0 > 5) {
    y = true;
}

x = 0;

function ostras(a: integer, b: boolean): integer {
    let yui = a;
    if (yui == a) {
        while (true) {
            const idk: string = "Funca";
            return yui;
        }
    }
}

while (1 > 10) {
    let lol = 0;
    lol = lol + x;
}

x = 0;

ostras(x, true);