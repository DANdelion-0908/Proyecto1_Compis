let a: integer = 100;
let b: integer = 20;
let c: integer = 5;
let d: integer = 2;
let e: integer = 7;
let f: integer = 3;
let g: integer = 10;
let h: integer = 11;

let resNum: integer = 0;
resNum = (a + b) - (c * d);
resNum = resNum + (e / f);
resNum = resNum * (g % h);
resNum = (resNum + a) - (b / c) + (d * e);